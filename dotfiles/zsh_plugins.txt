# ~/.zsh_plugins.txt — managed by ~/mac-setup, loaded by antidote.
# Add a plugin = add a line here, then run `reload`.

zsh-users/zsh-autosuggestions

# Oh My Zsh git plugin: gst, gco, gcb, gp, gl, glog, ... (maintained upstream)
# lib/git.zsh provides helper fns (git_current_branch, git_main_branch) the aliases use.
ohmyzsh/ohmyzsh path:lib/git.zsh
ohmyzsh/ohmyzsh path:plugins/git

# MUST be last.
zsh-users/zsh-syntax-highlighting
