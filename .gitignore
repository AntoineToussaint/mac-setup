# Transient logs from setup runs
*.log

# Dotfile backups created by setup.sh
*.bak-*
*.custom-bak

# Raycast exports — encrypted, but they bundle clipboard history, notes and AI
# chats along with the extension list. Keep them out of the repo.
raycast/
*.rayconfig
